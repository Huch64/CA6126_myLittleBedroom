# CPU-only torch wheels — HF Spaces free tier has no GPU, and the full CUDA
# wheel is ~2GB and slow to install / boot.
--extra-index-url https://download.pytorch.org/whl/cpu

gradio>=4.44,<5
sb3-contrib>=2.3
stable-baselines3>=2.3
gymnasium>=0.29,<2.0
torch>=2.0
numpy>=1.24
matplotlib>=3.7
imageio>=2.34
